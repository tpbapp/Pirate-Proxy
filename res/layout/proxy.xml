<?xml version="1.0" encoding="utf-8"?>
 
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:orientation="horizontal"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
	android:id="@+id/proxy">

    <ImageView
        android:id="@+id/proxy_icon"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_centerVertical="true"
	    android:layout_marginTop="8dp"
	    android:layout_marginBottom="8dp"
        android:contentDescription="@string/proxy"
        android:src="@drawable/neutral" />

	<TextView
	    android:id="@+id/proxy_url"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_toRightOf="@+id/proxy_icon"
	    android:layout_toLeftOf="@+id/proxy_rating"
	    android:layout_centerVertical="true"
	    android:paddingTop="8dp"
	    android:paddingBottom="8dp"
	    android:paddingRight="8dp"
	    android:paddingLeft="8dp"
	    android:text="@string/proxy"
	    android:textIsSelectable="false" />

	<TextView
	    android:id="@+id/proxy_rating"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_alignParentRight="true"
	    android:layout_centerVertical="true"
	    android:layout_marginTop="8dp"
	    android:layout_marginBottom="8dp"
	    android:textStyle="bold"
	    android:textIsSelectable="false" />

    <!-- <ImageView
        android:id="@+id/proxydelete"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_centerVertical="true"
	    android:layout_alignParentRight="true"
	    android:gravity="right"
	    android:paddingLeft="8dp"
        android:contentDescription="@string/deleteproxy"
        android:src="@drawable/delete" /> -->
 
</RelativeLayout>